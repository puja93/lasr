# Frontend Environment Variables
# Copy this file to .env.local and fill in your actual values
# NOTE: All frontend variables MUST be prefixed with VITE_ to be accessible

# Backend API endpoint (OPTIONAL)
# If not set, defaults to /api/connection-details (works for local development)
# For production, set this to your deployed backend URL
# VITE_CONN_DETAILS_ENDPOINT=https://your-backend.vercel.app/api/connection-details

# Mapbox access token (OPTIONAL - only if using Mapbox features)
# VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_token_here

# Other app configuration (OPTIONAL)
# VITE_APP_CONFIG_ENDPOINT=https://your-config-endpoint.com
# VITE_SANDBOX_ID=your_sandbox_id
